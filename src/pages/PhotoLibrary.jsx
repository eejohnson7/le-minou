import Typography from "@mui/material/Typography";

function PhotoLibrary() {
  return (
      <Typography
        variant="h2"
        sx={{
          fontSize: "3rem",
          marginBottom: "3rem",
          textAlign: "center"
        }}
      >
        Photo Library
      </Typography>
  );
}

export default PhotoLibrary;